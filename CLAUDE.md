@AGENTS.md

Claude must treat the imported instructions as repository requirements. Claude project-skill adapters live under `.claude/skills` and point to the canonical `.agents/skills` implementations.
